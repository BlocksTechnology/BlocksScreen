from __future__ import annotations

import logging
import re
from pathlib import Path

from .models import ComponentConfig, ComponentStatus

__all__ = ["ComponentConfig", "ComponentStatus", "load_components"]

logger = logging.getLogger(__name__)

_SERVICE_RE = re.compile(r"^[a-zA-Z0-9@:._-]+\.service$")
_GIT_BRANCH_RE = re.compile(r"^[a-zA-Z0-9][a-zA-Z0-9._/-]*$")
_GIT_VERSION_RE = re.compile(r"^[a-zA-Z0-9][a-zA-Z0-9._/+-]*$")
_COMPONENT_NAME_RE = re.compile(r"^[a-zA-Z0-9][a-zA-Z0-9_-]*$")
_GIT_URL_RE = re.compile(r"^https://[a-zA-Z0-9._~:/?#\[\]@!$&'()*+,;=%-]+$")
_SERVICE_BANNED = set("/\\;&|$`") | {" ", "\t"}
OVERRIDE_PATH = Path("~/printer_data/config/blockscreen_updater.yaml").expanduser()

# Unioned into EVERY apt component: a kernel/firmware bump is unrecoverable on a 1-partition no-SSH Pi.
_KERNEL_FIRMWARE_EXCLUDES: tuple[str, ...] = (
    "^linux-image",
    "^linux-headers",
    "^raspberrypi-",
    "^firmware-",
)


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_x__validate_service__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__validate_service__mutmut)
def _validate_service(name: str) -> bool:
    """Return True if name is a safe, well-formed systemd .service unit name."""
    if not name:
        return False
    if any(ch in _SERVICE_BANNED for ch in name):
        return False
    return bool(_SERVICE_RE.match(name))


def x__validate_service__mutmut_orig(name: str) -> bool:
    """Return True if name is a safe, well-formed systemd .service unit name."""
    if not name:
        return False
    if any(ch in _SERVICE_BANNED for ch in name):
        return False
    return bool(_SERVICE_RE.match(name))


def x__validate_service__mutmut_1(name: str) -> bool:
    """Return True if name is a safe, well-formed systemd .service unit name."""
    if name:
        return False
    if any(ch in _SERVICE_BANNED for ch in name):
        return False
    return bool(_SERVICE_RE.match(name))


def x__validate_service__mutmut_2(name: str) -> bool:
    """Return True if name is a safe, well-formed systemd .service unit name."""
    if not name:
        return True
    if any(ch in _SERVICE_BANNED for ch in name):
        return False
    return bool(_SERVICE_RE.match(name))


def x__validate_service__mutmut_3(name: str) -> bool:
    """Return True if name is a safe, well-formed systemd .service unit name."""
    if not name:
        return False
    if any(None):
        return False
    return bool(_SERVICE_RE.match(name))


def x__validate_service__mutmut_4(name: str) -> bool:
    """Return True if name is a safe, well-formed systemd .service unit name."""
    if not name:
        return False
    if any(ch not in _SERVICE_BANNED for ch in name):
        return False
    return bool(_SERVICE_RE.match(name))


def x__validate_service__mutmut_5(name: str) -> bool:
    """Return True if name is a safe, well-formed systemd .service unit name."""
    if not name:
        return False
    if any(ch in _SERVICE_BANNED for ch in name):
        return True
    return bool(_SERVICE_RE.match(name))


def x__validate_service__mutmut_6(name: str) -> bool:
    """Return True if name is a safe, well-formed systemd .service unit name."""
    if not name:
        return False
    if any(ch in _SERVICE_BANNED for ch in name):
        return False
    return bool(None)


def x__validate_service__mutmut_7(name: str) -> bool:
    """Return True if name is a safe, well-formed systemd .service unit name."""
    if not name:
        return False
    if any(ch in _SERVICE_BANNED for ch in name):
        return False
    return bool(_SERVICE_RE.match(None))

mutants_x__validate_service__mutmut['_mutmut_orig'] = x__validate_service__mutmut_orig # type: ignore # mutmut generated
mutants_x__validate_service__mutmut['x__validate_service__mutmut_1'] = x__validate_service__mutmut_1 # type: ignore # mutmut generated
mutants_x__validate_service__mutmut['x__validate_service__mutmut_2'] = x__validate_service__mutmut_2 # type: ignore # mutmut generated
mutants_x__validate_service__mutmut['x__validate_service__mutmut_3'] = x__validate_service__mutmut_3 # type: ignore # mutmut generated
mutants_x__validate_service__mutmut['x__validate_service__mutmut_4'] = x__validate_service__mutmut_4 # type: ignore # mutmut generated
mutants_x__validate_service__mutmut['x__validate_service__mutmut_5'] = x__validate_service__mutmut_5 # type: ignore # mutmut generated
mutants_x__validate_service__mutmut['x__validate_service__mutmut_6'] = x__validate_service__mutmut_6 # type: ignore # mutmut generated
mutants_x__validate_service__mutmut['x__validate_service__mutmut_7'] = x__validate_service__mutmut_7 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__validate_component__mutmut)
def _validate_component(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_orig(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_1(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = None
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_2(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get(None, "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_3(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", None)
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_4(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_5(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", )
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_6(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("XXnameXX", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_7(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("NAME", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_8(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "XXXX")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_9(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) and not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_10(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_11(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_12(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning(None)
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_13(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("XXComponent missing name,  skippedXX")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_14(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_15(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("COMPONENT MISSING NAME,  SKIPPED")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_16(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) >= 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_17(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 256:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_18(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning(None, len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_19(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", None)
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_20(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning(len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_21(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", )
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_22(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("XXComponent name too long (%d chars) - skippedXX", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_23(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_24(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("COMPONENT NAME TOO LONG (%D CHARS) - SKIPPED", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_25(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_26(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(None):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_27(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            None, name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_28(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", None
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_29(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_30(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_31(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "XXComponent %r has invalid name (must match [a-zA-Z0-9_-]+) - skippedXX", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_32(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "component %r has invalid name (must match [a-za-z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_33(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "COMPONENT %R HAS INVALID NAME (MUST MATCH [A-ZA-Z0-9_-]+) - SKIPPED", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_34(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = None
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_35(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get(None, "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_36(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", None)
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_37(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_38(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", )
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_39(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("XXtypeXX", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_40(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("TYPE", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_41(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "XXXX")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_42(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_43(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("XXgitXX", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_44(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("GIT", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_45(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "XXaptXX"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_46(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "APT"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_47(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning(None, name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_48(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", None, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_49(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, None)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_50(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning(name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_51(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_52(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, )
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_53(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("XXComponent %r has invalid type %r - skippedXX", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_54(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_55(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("COMPONENT %R HAS INVALID TYPE %R - SKIPPED", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_56(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type != "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_57(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "XXgitXX":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_58(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "GIT":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_59(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = None
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_60(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get(None)
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_61(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("XXpathXX")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_62(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("PATH")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_63(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_64(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning(None, name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_65(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", None)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_66(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning(name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_67(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", )
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_68(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("XXComponent %r missing path - skippedXX", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_69(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_70(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("COMPONENT %R MISSING PATH - SKIPPED", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_71(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = None
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_72(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(None).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_73(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(None)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_74(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_75(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(None):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_76(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning(None, name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_77(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", None)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_78(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning(name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_79(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", )
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_80(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("XXComponent %r path escapes home dir - skippedXX", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_81(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_82(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("COMPONENT %R PATH ESCAPES HOME DIR - SKIPPED", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_83(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = None
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_84(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get(None)
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_85(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("XXserviceXX")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_86(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("SERVICE")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_87(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None or not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_88(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_89(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_90(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(None):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_91(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(None)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_92(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                None, name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_93(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", None, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_94(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, None
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_95(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_96(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_97(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_98(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "XXComponent %r has invalid service %r, skippedXX", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_99(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_100(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "COMPONENT %R HAS INVALID SERVICE %R, SKIPPED", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_101(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = None
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_102(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get(None)
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_103(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("XXbranchXX")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_104(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("BRANCH")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_105(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_106(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = None
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_107(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(None)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_108(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith(None):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_109(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("XXorigin/XX"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_110(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("ORIGIN/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_111(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    None,
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_112(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    None,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_113(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    None,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_114(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_115(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_116(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_117(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "XXComponent %r branch %r has an 'origin/' prefix - stripping itXX",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_118(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_119(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "COMPONENT %R BRANCH %R HAS AN 'ORIGIN/' PREFIX - STRIPPING IT",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_120(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = None
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_121(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_122(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(None):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_123(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    None, name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_124(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", None, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_125(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, None
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_126(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_127(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_128(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_129(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "XXComponent %r has invalid branch name %r, skippedXX", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_130(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_131(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "COMPONENT %R HAS INVALID BRANCH NAME %R, SKIPPED", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_132(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = None
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_133(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get(None)
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_134(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("XXversionXX")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_135(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("VERSION")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_136(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None or not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_137(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_138(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_139(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(None):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_140(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(None)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_141(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                None,
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_142(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                None,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_143(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                None,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_144(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                None,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_145(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_146(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_147(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_148(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_149(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "XXComponent %r has invalid version[%s] for the branch %sXX",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_150(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_151(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "COMPONENT %R HAS INVALID VERSION[%S] FOR THE BRANCH %S",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_152(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = None
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_153(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 51
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_154(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = None
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_155(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(None)
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_156(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get(None, 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_157(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", None))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_158(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get(50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_159(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", ))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_160(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("XXorderXX", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_161(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("ORDER", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_162(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 51))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_163(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning(None, name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_164(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", None)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_165(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning(name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_166(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", )

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_167(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("XXComponent %r has invalid order valueXX", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_168(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_169(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("COMPONENT %R HAS INVALID ORDER VALUE", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_170(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = None
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_171(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get(None)
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_172(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("XXurlXX")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_173(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("URL")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_174(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None or not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_175(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_176(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_177(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(None):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_178(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(None)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_179(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                None, name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_180(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", None, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_181(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, None
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_182(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_183(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_184(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_185(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "XXComponent %r has invalid (non-https) url %r - dropping urlXX", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_186(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_187(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "COMPONENT %R HAS INVALID (NON-HTTPS) URL %R - DROPPING URL", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_188(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = ""

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_189(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = None
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_190(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get(None, "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_191(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", None)
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_192(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_193(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", )
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_194(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("XXreset_modeXX", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_195(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("RESET_MODE", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_196(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "XXhardXX")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_197(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "HARD")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_198(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_199(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("XXhardXX", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_200(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("HARD", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_201(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "XXsoftXX"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_202(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "SOFT"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_203(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                None,
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_204(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                None,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_205(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                None,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_206(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_207(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_208(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_209(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "XXComponent %r has invalid reset_mode %r - using 'hard'XX",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_210(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_211(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "COMPONENT %R HAS INVALID RESET_MODE %R - USING 'HARD'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_212(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = None

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_213(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "XXhardXX"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_214(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "HARD"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_215(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = None
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_216(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(None)
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_217(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get(None, False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_218(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", None))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_219(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get(False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_220(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", ))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_221(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("XXinstall_if_missingXX", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_222(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("INSTALL_IF_MISSING", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_223(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", True))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_224(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing or not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_225(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_226(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                None,
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_227(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                None,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_228(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_229(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_230(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "XXComponent %r sets install_if_missing but has no valid url - XX"
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_231(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_232(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "COMPONENT %R SETS INSTALL_IF_MISSING BUT HAS NO VALID URL - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_233(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "XXcannot provision, disablingXX",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_234(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "CANNOT PROVISION, DISABLING",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_235(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = None

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_236(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = True

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_237(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=None,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_238(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=None,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_239(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=None,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_240(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_241(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=None,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_242(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=None,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_243(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_244(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_245(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_246(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=None,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_247(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=None,
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_248(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=None,
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_249(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_250(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_251(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_252(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_253(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_254(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_255(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_256(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_257(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_258(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_259(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_260(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_261(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(None) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_262(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(None) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_263(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(None) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_264(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(None) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_265(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(None),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_266(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get(None, False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_267(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", None)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_268(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get(False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_269(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", )),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_270(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("XXrestart_uiXX", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_271(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("RESTART_UI", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_272(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", True)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_273(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(None),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_274(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get(None, False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_275(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", None)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_276(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get(False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_277(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", )),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_278(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("XXrestart_klipperXX", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_279(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("RESTART_KLIPPER", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_280(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", True)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_281(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = None
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_282(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 51
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_283(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = None
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_284(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(None)
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_285(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get(None, 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_286(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", None))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_287(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get(50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_288(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", ))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_289(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("XXorderXX", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_290(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("ORDER", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_291(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 51))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_292(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning(None, name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_293(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", None)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_294(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning(name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_295(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", )
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_296(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("XXComponent %r has invalid order valueXX", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_297(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_298(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("COMPONENT %R HAS INVALID ORDER VALUE", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_299(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = None
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_300(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get(None, [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_301(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", None)
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_302(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get([])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_303(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", )
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_304(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("XXapt_excludeXX", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_305(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("APT_EXCLUDE", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_306(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = None
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_307(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = None
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_308(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(None)
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_309(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(None) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_310(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = None
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_311(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES - tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_312(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        None
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_313(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_314(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=None,
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_315(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind=None,
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_316(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=None,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_317(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        apt_exclude=None,
    )


def x__validate_component__mutmut_318(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        kind="apt",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_319(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_320(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_321(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="apt",
        order=apt_order,
        )


def x__validate_component__mutmut_322(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="XXaptXX",
        order=apt_order,
        apt_exclude=apt_exclude,
    )


def x__validate_component__mutmut_323(data: dict) -> ComponentConfig | None:
    """Parse and validate a raw component dict; returns None and logs a warning on any error."""  # noqa: E501
    name = data.get("name", "")
    if not isinstance(name, str) or not name:
        logger.warning("Component missing name,  skipped")
        return None
    if len(name) > 255:
        logger.warning("Component name too long (%d chars) - skipped", len(name))
        return None
    if not _COMPONENT_NAME_RE.match(name):
        logger.warning(
            "Component %r has invalid name (must match [a-zA-Z0-9_-]+) - skipped", name
        )
        return None

    comp_type = data.get("type", "")
    if comp_type not in ("git", "apt"):
        logger.warning("Component %r has invalid type %r - skipped", name, comp_type)
        return None

    if comp_type == "git":
        raw_path = data.get("path")
        if not raw_path:
            logger.warning("Component %r missing path - skipped", name)
            return None
        resolved = Path(str(raw_path)).expanduser().resolve()
        if not resolved.is_relative_to(Path.home()):
            logger.warning("Component %r path escapes home dir - skipped", name)
            return None

        service = data.get("service")
        if service is not None and not _validate_service(str(service)):
            logger.warning(
                "Component %r has invalid service %r, skipped", name, service
            )
            return None

        branch = data.get("branch")
        if branch is not None:
            branch = str(branch)
            # Strip a stray remote prefix: `origin/x` would fetch `origin/origin/x`.
            if branch.startswith("origin/"):
                logger.warning(
                    "Component %r branch %r has an 'origin/' prefix - stripping it",
                    name,
                    branch,
                )
                branch = branch[len("origin/") :]
            if not _GIT_BRANCH_RE.match(branch):
                logger.warning(
                    "Component %r has invalid branch name %r, skipped", name, branch
                )
                return None

        version = data.get("version")
        if version is not None and not _GIT_VERSION_RE.match(str(version)):
            logger.warning(
                "Component %r has invalid version[%s] for the branch %s",
                name,
                version,
                branch,
            )
            return None

        order = 50
        try:
            order = int(data.get("order", 50))
        except (TypeError, ValueError):
            logger.warning("Component %r has invalid order value", name)

        url = data.get("url")
        if url is not None and not _GIT_URL_RE.match(str(url)):
            logger.warning(
                "Component %r has invalid (non-https) url %r - dropping url", name, url
            )
            url = None

        reset_mode = data.get("reset_mode", "hard")
        if reset_mode not in ("hard", "soft"):
            # An unknown value must not silently take the soft path (fleet default is hard).
            logger.warning(
                "Component %r has invalid reset_mode %r - using 'hard'",
                name,
                reset_mode,
            )
            reset_mode = "hard"

        install_if_missing = bool(data.get("install_if_missing", False))
        if install_if_missing and not url:
            logger.warning(
                "Component %r sets install_if_missing but has no valid url - "
                "cannot provision, disabling",
                name,
            )
            install_if_missing = False

        return ComponentConfig(
            name=name,
            kind=comp_type,
            path=resolved,
            service=str(service) if service else None,
            reset_mode=reset_mode,
            order=order,
            branch=str(branch) if branch else None,
            version=str(version) if version else None,
            url=str(url) if url else None,
            install_if_missing=install_if_missing,
            restart_ui=bool(data.get("restart_ui", False)),
            restart_klipper=bool(data.get("restart_klipper", False)),
        )
    apt_order = 50
    try:
        apt_order = int(data.get("order", 50))
    except (TypeError, ValueError):
        logger.warning("Component %r has invalid order value", name)
    raw_exclude = data.get("apt_exclude", [])
    apt_exclude: tuple[str, ...] = ()
    if isinstance(raw_exclude, list):
        apt_exclude = tuple(str(p) for p in raw_exclude if isinstance(p, str))
    # Kernel/firmware guard is non-negotiable: prepend it, drop any duplicates.
    apt_exclude = _KERNEL_FIRMWARE_EXCLUDES + tuple(
        p for p in apt_exclude if p not in _KERNEL_FIRMWARE_EXCLUDES
    )
    return ComponentConfig(
        name=name,
        kind="APT",
        order=apt_order,
        apt_exclude=apt_exclude,
    )

mutants_x__validate_component__mutmut['_mutmut_orig'] = x__validate_component__mutmut_orig # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_1'] = x__validate_component__mutmut_1 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_2'] = x__validate_component__mutmut_2 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_3'] = x__validate_component__mutmut_3 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_4'] = x__validate_component__mutmut_4 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_5'] = x__validate_component__mutmut_5 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_6'] = x__validate_component__mutmut_6 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_7'] = x__validate_component__mutmut_7 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_8'] = x__validate_component__mutmut_8 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_9'] = x__validate_component__mutmut_9 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_10'] = x__validate_component__mutmut_10 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_11'] = x__validate_component__mutmut_11 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_12'] = x__validate_component__mutmut_12 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_13'] = x__validate_component__mutmut_13 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_14'] = x__validate_component__mutmut_14 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_15'] = x__validate_component__mutmut_15 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_16'] = x__validate_component__mutmut_16 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_17'] = x__validate_component__mutmut_17 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_18'] = x__validate_component__mutmut_18 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_19'] = x__validate_component__mutmut_19 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_20'] = x__validate_component__mutmut_20 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_21'] = x__validate_component__mutmut_21 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_22'] = x__validate_component__mutmut_22 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_23'] = x__validate_component__mutmut_23 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_24'] = x__validate_component__mutmut_24 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_25'] = x__validate_component__mutmut_25 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_26'] = x__validate_component__mutmut_26 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_27'] = x__validate_component__mutmut_27 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_28'] = x__validate_component__mutmut_28 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_29'] = x__validate_component__mutmut_29 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_30'] = x__validate_component__mutmut_30 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_31'] = x__validate_component__mutmut_31 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_32'] = x__validate_component__mutmut_32 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_33'] = x__validate_component__mutmut_33 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_34'] = x__validate_component__mutmut_34 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_35'] = x__validate_component__mutmut_35 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_36'] = x__validate_component__mutmut_36 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_37'] = x__validate_component__mutmut_37 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_38'] = x__validate_component__mutmut_38 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_39'] = x__validate_component__mutmut_39 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_40'] = x__validate_component__mutmut_40 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_41'] = x__validate_component__mutmut_41 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_42'] = x__validate_component__mutmut_42 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_43'] = x__validate_component__mutmut_43 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_44'] = x__validate_component__mutmut_44 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_45'] = x__validate_component__mutmut_45 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_46'] = x__validate_component__mutmut_46 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_47'] = x__validate_component__mutmut_47 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_48'] = x__validate_component__mutmut_48 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_49'] = x__validate_component__mutmut_49 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_50'] = x__validate_component__mutmut_50 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_51'] = x__validate_component__mutmut_51 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_52'] = x__validate_component__mutmut_52 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_53'] = x__validate_component__mutmut_53 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_54'] = x__validate_component__mutmut_54 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_55'] = x__validate_component__mutmut_55 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_56'] = x__validate_component__mutmut_56 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_57'] = x__validate_component__mutmut_57 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_58'] = x__validate_component__mutmut_58 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_59'] = x__validate_component__mutmut_59 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_60'] = x__validate_component__mutmut_60 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_61'] = x__validate_component__mutmut_61 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_62'] = x__validate_component__mutmut_62 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_63'] = x__validate_component__mutmut_63 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_64'] = x__validate_component__mutmut_64 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_65'] = x__validate_component__mutmut_65 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_66'] = x__validate_component__mutmut_66 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_67'] = x__validate_component__mutmut_67 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_68'] = x__validate_component__mutmut_68 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_69'] = x__validate_component__mutmut_69 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_70'] = x__validate_component__mutmut_70 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_71'] = x__validate_component__mutmut_71 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_72'] = x__validate_component__mutmut_72 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_73'] = x__validate_component__mutmut_73 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_74'] = x__validate_component__mutmut_74 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_75'] = x__validate_component__mutmut_75 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_76'] = x__validate_component__mutmut_76 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_77'] = x__validate_component__mutmut_77 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_78'] = x__validate_component__mutmut_78 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_79'] = x__validate_component__mutmut_79 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_80'] = x__validate_component__mutmut_80 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_81'] = x__validate_component__mutmut_81 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_82'] = x__validate_component__mutmut_82 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_83'] = x__validate_component__mutmut_83 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_84'] = x__validate_component__mutmut_84 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_85'] = x__validate_component__mutmut_85 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_86'] = x__validate_component__mutmut_86 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_87'] = x__validate_component__mutmut_87 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_88'] = x__validate_component__mutmut_88 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_89'] = x__validate_component__mutmut_89 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_90'] = x__validate_component__mutmut_90 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_91'] = x__validate_component__mutmut_91 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_92'] = x__validate_component__mutmut_92 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_93'] = x__validate_component__mutmut_93 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_94'] = x__validate_component__mutmut_94 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_95'] = x__validate_component__mutmut_95 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_96'] = x__validate_component__mutmut_96 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_97'] = x__validate_component__mutmut_97 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_98'] = x__validate_component__mutmut_98 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_99'] = x__validate_component__mutmut_99 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_100'] = x__validate_component__mutmut_100 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_101'] = x__validate_component__mutmut_101 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_102'] = x__validate_component__mutmut_102 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_103'] = x__validate_component__mutmut_103 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_104'] = x__validate_component__mutmut_104 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_105'] = x__validate_component__mutmut_105 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_106'] = x__validate_component__mutmut_106 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_107'] = x__validate_component__mutmut_107 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_108'] = x__validate_component__mutmut_108 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_109'] = x__validate_component__mutmut_109 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_110'] = x__validate_component__mutmut_110 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_111'] = x__validate_component__mutmut_111 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_112'] = x__validate_component__mutmut_112 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_113'] = x__validate_component__mutmut_113 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_114'] = x__validate_component__mutmut_114 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_115'] = x__validate_component__mutmut_115 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_116'] = x__validate_component__mutmut_116 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_117'] = x__validate_component__mutmut_117 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_118'] = x__validate_component__mutmut_118 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_119'] = x__validate_component__mutmut_119 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_120'] = x__validate_component__mutmut_120 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_121'] = x__validate_component__mutmut_121 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_122'] = x__validate_component__mutmut_122 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_123'] = x__validate_component__mutmut_123 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_124'] = x__validate_component__mutmut_124 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_125'] = x__validate_component__mutmut_125 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_126'] = x__validate_component__mutmut_126 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_127'] = x__validate_component__mutmut_127 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_128'] = x__validate_component__mutmut_128 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_129'] = x__validate_component__mutmut_129 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_130'] = x__validate_component__mutmut_130 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_131'] = x__validate_component__mutmut_131 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_132'] = x__validate_component__mutmut_132 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_133'] = x__validate_component__mutmut_133 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_134'] = x__validate_component__mutmut_134 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_135'] = x__validate_component__mutmut_135 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_136'] = x__validate_component__mutmut_136 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_137'] = x__validate_component__mutmut_137 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_138'] = x__validate_component__mutmut_138 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_139'] = x__validate_component__mutmut_139 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_140'] = x__validate_component__mutmut_140 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_141'] = x__validate_component__mutmut_141 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_142'] = x__validate_component__mutmut_142 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_143'] = x__validate_component__mutmut_143 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_144'] = x__validate_component__mutmut_144 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_145'] = x__validate_component__mutmut_145 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_146'] = x__validate_component__mutmut_146 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_147'] = x__validate_component__mutmut_147 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_148'] = x__validate_component__mutmut_148 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_149'] = x__validate_component__mutmut_149 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_150'] = x__validate_component__mutmut_150 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_151'] = x__validate_component__mutmut_151 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_152'] = x__validate_component__mutmut_152 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_153'] = x__validate_component__mutmut_153 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_154'] = x__validate_component__mutmut_154 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_155'] = x__validate_component__mutmut_155 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_156'] = x__validate_component__mutmut_156 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_157'] = x__validate_component__mutmut_157 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_158'] = x__validate_component__mutmut_158 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_159'] = x__validate_component__mutmut_159 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_160'] = x__validate_component__mutmut_160 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_161'] = x__validate_component__mutmut_161 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_162'] = x__validate_component__mutmut_162 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_163'] = x__validate_component__mutmut_163 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_164'] = x__validate_component__mutmut_164 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_165'] = x__validate_component__mutmut_165 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_166'] = x__validate_component__mutmut_166 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_167'] = x__validate_component__mutmut_167 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_168'] = x__validate_component__mutmut_168 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_169'] = x__validate_component__mutmut_169 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_170'] = x__validate_component__mutmut_170 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_171'] = x__validate_component__mutmut_171 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_172'] = x__validate_component__mutmut_172 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_173'] = x__validate_component__mutmut_173 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_174'] = x__validate_component__mutmut_174 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_175'] = x__validate_component__mutmut_175 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_176'] = x__validate_component__mutmut_176 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_177'] = x__validate_component__mutmut_177 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_178'] = x__validate_component__mutmut_178 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_179'] = x__validate_component__mutmut_179 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_180'] = x__validate_component__mutmut_180 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_181'] = x__validate_component__mutmut_181 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_182'] = x__validate_component__mutmut_182 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_183'] = x__validate_component__mutmut_183 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_184'] = x__validate_component__mutmut_184 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_185'] = x__validate_component__mutmut_185 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_186'] = x__validate_component__mutmut_186 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_187'] = x__validate_component__mutmut_187 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_188'] = x__validate_component__mutmut_188 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_189'] = x__validate_component__mutmut_189 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_190'] = x__validate_component__mutmut_190 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_191'] = x__validate_component__mutmut_191 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_192'] = x__validate_component__mutmut_192 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_193'] = x__validate_component__mutmut_193 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_194'] = x__validate_component__mutmut_194 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_195'] = x__validate_component__mutmut_195 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_196'] = x__validate_component__mutmut_196 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_197'] = x__validate_component__mutmut_197 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_198'] = x__validate_component__mutmut_198 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_199'] = x__validate_component__mutmut_199 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_200'] = x__validate_component__mutmut_200 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_201'] = x__validate_component__mutmut_201 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_202'] = x__validate_component__mutmut_202 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_203'] = x__validate_component__mutmut_203 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_204'] = x__validate_component__mutmut_204 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_205'] = x__validate_component__mutmut_205 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_206'] = x__validate_component__mutmut_206 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_207'] = x__validate_component__mutmut_207 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_208'] = x__validate_component__mutmut_208 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_209'] = x__validate_component__mutmut_209 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_210'] = x__validate_component__mutmut_210 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_211'] = x__validate_component__mutmut_211 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_212'] = x__validate_component__mutmut_212 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_213'] = x__validate_component__mutmut_213 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_214'] = x__validate_component__mutmut_214 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_215'] = x__validate_component__mutmut_215 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_216'] = x__validate_component__mutmut_216 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_217'] = x__validate_component__mutmut_217 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_218'] = x__validate_component__mutmut_218 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_219'] = x__validate_component__mutmut_219 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_220'] = x__validate_component__mutmut_220 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_221'] = x__validate_component__mutmut_221 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_222'] = x__validate_component__mutmut_222 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_223'] = x__validate_component__mutmut_223 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_224'] = x__validate_component__mutmut_224 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_225'] = x__validate_component__mutmut_225 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_226'] = x__validate_component__mutmut_226 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_227'] = x__validate_component__mutmut_227 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_228'] = x__validate_component__mutmut_228 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_229'] = x__validate_component__mutmut_229 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_230'] = x__validate_component__mutmut_230 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_231'] = x__validate_component__mutmut_231 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_232'] = x__validate_component__mutmut_232 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_233'] = x__validate_component__mutmut_233 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_234'] = x__validate_component__mutmut_234 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_235'] = x__validate_component__mutmut_235 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_236'] = x__validate_component__mutmut_236 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_237'] = x__validate_component__mutmut_237 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_238'] = x__validate_component__mutmut_238 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_239'] = x__validate_component__mutmut_239 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_240'] = x__validate_component__mutmut_240 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_241'] = x__validate_component__mutmut_241 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_242'] = x__validate_component__mutmut_242 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_243'] = x__validate_component__mutmut_243 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_244'] = x__validate_component__mutmut_244 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_245'] = x__validate_component__mutmut_245 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_246'] = x__validate_component__mutmut_246 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_247'] = x__validate_component__mutmut_247 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_248'] = x__validate_component__mutmut_248 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_249'] = x__validate_component__mutmut_249 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_250'] = x__validate_component__mutmut_250 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_251'] = x__validate_component__mutmut_251 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_252'] = x__validate_component__mutmut_252 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_253'] = x__validate_component__mutmut_253 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_254'] = x__validate_component__mutmut_254 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_255'] = x__validate_component__mutmut_255 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_256'] = x__validate_component__mutmut_256 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_257'] = x__validate_component__mutmut_257 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_258'] = x__validate_component__mutmut_258 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_259'] = x__validate_component__mutmut_259 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_260'] = x__validate_component__mutmut_260 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_261'] = x__validate_component__mutmut_261 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_262'] = x__validate_component__mutmut_262 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_263'] = x__validate_component__mutmut_263 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_264'] = x__validate_component__mutmut_264 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_265'] = x__validate_component__mutmut_265 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_266'] = x__validate_component__mutmut_266 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_267'] = x__validate_component__mutmut_267 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_268'] = x__validate_component__mutmut_268 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_269'] = x__validate_component__mutmut_269 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_270'] = x__validate_component__mutmut_270 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_271'] = x__validate_component__mutmut_271 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_272'] = x__validate_component__mutmut_272 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_273'] = x__validate_component__mutmut_273 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_274'] = x__validate_component__mutmut_274 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_275'] = x__validate_component__mutmut_275 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_276'] = x__validate_component__mutmut_276 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_277'] = x__validate_component__mutmut_277 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_278'] = x__validate_component__mutmut_278 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_279'] = x__validate_component__mutmut_279 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_280'] = x__validate_component__mutmut_280 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_281'] = x__validate_component__mutmut_281 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_282'] = x__validate_component__mutmut_282 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_283'] = x__validate_component__mutmut_283 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_284'] = x__validate_component__mutmut_284 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_285'] = x__validate_component__mutmut_285 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_286'] = x__validate_component__mutmut_286 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_287'] = x__validate_component__mutmut_287 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_288'] = x__validate_component__mutmut_288 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_289'] = x__validate_component__mutmut_289 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_290'] = x__validate_component__mutmut_290 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_291'] = x__validate_component__mutmut_291 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_292'] = x__validate_component__mutmut_292 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_293'] = x__validate_component__mutmut_293 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_294'] = x__validate_component__mutmut_294 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_295'] = x__validate_component__mutmut_295 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_296'] = x__validate_component__mutmut_296 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_297'] = x__validate_component__mutmut_297 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_298'] = x__validate_component__mutmut_298 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_299'] = x__validate_component__mutmut_299 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_300'] = x__validate_component__mutmut_300 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_301'] = x__validate_component__mutmut_301 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_302'] = x__validate_component__mutmut_302 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_303'] = x__validate_component__mutmut_303 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_304'] = x__validate_component__mutmut_304 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_305'] = x__validate_component__mutmut_305 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_306'] = x__validate_component__mutmut_306 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_307'] = x__validate_component__mutmut_307 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_308'] = x__validate_component__mutmut_308 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_309'] = x__validate_component__mutmut_309 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_310'] = x__validate_component__mutmut_310 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_311'] = x__validate_component__mutmut_311 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_312'] = x__validate_component__mutmut_312 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_313'] = x__validate_component__mutmut_313 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_314'] = x__validate_component__mutmut_314 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_315'] = x__validate_component__mutmut_315 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_316'] = x__validate_component__mutmut_316 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_317'] = x__validate_component__mutmut_317 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_318'] = x__validate_component__mutmut_318 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_319'] = x__validate_component__mutmut_319 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_320'] = x__validate_component__mutmut_320 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_321'] = x__validate_component__mutmut_321 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_322'] = x__validate_component__mutmut_322 # type: ignore # mutmut generated
mutants_x__validate_component__mutmut['x__validate_component__mutmut_323'] = x__validate_component__mutmut_323 # type: ignore # mutmut generated
mutants_x__merge__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__merge__mutmut)
def _merge(base: list[dict], override: list[dict]) -> list[dict]:
    """Merge override entries into base by name; unknown override names are appended."""
    base_by_name = {
        c["name"]: dict(c) for c in base if isinstance(c, dict) and "name" in c
    }
    for entry in override:
        if not isinstance(entry, dict):
            continue
        entry_name = entry.get("name")
        if not entry_name:
            continue
        if entry_name in base_by_name:
            for k, v in entry.items():
                if v is not None:
                    base_by_name[entry_name][k] = v
        else:
            base_by_name[entry_name] = dict(entry)
    return list(base_by_name.values())


def x__merge__mutmut_orig(base: list[dict], override: list[dict]) -> list[dict]:
    """Merge override entries into base by name; unknown override names are appended."""
    base_by_name = {
        c["name"]: dict(c) for c in base if isinstance(c, dict) and "name" in c
    }
    for entry in override:
        if not isinstance(entry, dict):
            continue
        entry_name = entry.get("name")
        if not entry_name:
            continue
        if entry_name in base_by_name:
            for k, v in entry.items():
                if v is not None:
                    base_by_name[entry_name][k] = v
        else:
            base_by_name[entry_name] = dict(entry)
    return list(base_by_name.values())


def x__merge__mutmut_1(base: list[dict], override: list[dict]) -> list[dict]:
    """Merge override entries into base by name; unknown override names are appended."""
    base_by_name = None
    for entry in override:
        if not isinstance(entry, dict):
            continue
        entry_name = entry.get("name")
        if not entry_name:
            continue
        if entry_name in base_by_name:
            for k, v in entry.items():
                if v is not None:
                    base_by_name[entry_name][k] = v
        else:
            base_by_name[entry_name] = dict(entry)
    return list(base_by_name.values())


def x__merge__mutmut_2(base: list[dict], override: list[dict]) -> list[dict]:
    """Merge override entries into base by name; unknown override names are appended."""
    base_by_name = {
        c["XXnameXX"]: dict(c) for c in base if isinstance(c, dict) and "name" in c
    }
    for entry in override:
        if not isinstance(entry, dict):
            continue
        entry_name = entry.get("name")
        if not entry_name:
            continue
        if entry_name in base_by_name:
            for k, v in entry.items():
                if v is not None:
                    base_by_name[entry_name][k] = v
        else:
            base_by_name[entry_name] = dict(entry)
    return list(base_by_name.values())


def x__merge__mutmut_3(base: list[dict], override: list[dict]) -> list[dict]:
    """Merge override entries into base by name; unknown override names are appended."""
    base_by_name = {
        c["NAME"]: dict(c) for c in base if isinstance(c, dict) and "name" in c
    }
    for entry in override:
        if not isinstance(entry, dict):
            continue
        entry_name = entry.get("name")
        if not entry_name:
            continue
        if entry_name in base_by_name:
            for k, v in entry.items():
                if v is not None:
                    base_by_name[entry_name][k] = v
        else:
            base_by_name[entry_name] = dict(entry)
    return list(base_by_name.values())


def x__merge__mutmut_4(base: list[dict], override: list[dict]) -> list[dict]:
    """Merge override entries into base by name; unknown override names are appended."""
    base_by_name = {
        c["name"]: dict(None) for c in base if isinstance(c, dict) and "name" in c
    }
    for entry in override:
        if not isinstance(entry, dict):
            continue
        entry_name = entry.get("name")
        if not entry_name:
            continue
        if entry_name in base_by_name:
            for k, v in entry.items():
                if v is not None:
                    base_by_name[entry_name][k] = v
        else:
            base_by_name[entry_name] = dict(entry)
    return list(base_by_name.values())


def x__merge__mutmut_5(base: list[dict], override: list[dict]) -> list[dict]:
    """Merge override entries into base by name; unknown override names are appended."""
    base_by_name = {
        c["name"]: dict(c) for c in base if isinstance(c, dict) or "name" in c
    }
    for entry in override:
        if not isinstance(entry, dict):
            continue
        entry_name = entry.get("name")
        if not entry_name:
            continue
        if entry_name in base_by_name:
            for k, v in entry.items():
                if v is not None:
                    base_by_name[entry_name][k] = v
        else:
            base_by_name[entry_name] = dict(entry)
    return list(base_by_name.values())


def x__merge__mutmut_6(base: list[dict], override: list[dict]) -> list[dict]:
    """Merge override entries into base by name; unknown override names are appended."""
    base_by_name = {
        c["name"]: dict(c) for c in base if isinstance(c, dict) and "XXnameXX" in c
    }
    for entry in override:
        if not isinstance(entry, dict):
            continue
        entry_name = entry.get("name")
        if not entry_name:
            continue
        if entry_name in base_by_name:
            for k, v in entry.items():
                if v is not None:
                    base_by_name[entry_name][k] = v
        else:
            base_by_name[entry_name] = dict(entry)
    return list(base_by_name.values())


def x__merge__mutmut_7(base: list[dict], override: list[dict]) -> list[dict]:
    """Merge override entries into base by name; unknown override names are appended."""
    base_by_name = {
        c["name"]: dict(c) for c in base if isinstance(c, dict) and "NAME" in c
    }
    for entry in override:
        if not isinstance(entry, dict):
            continue
        entry_name = entry.get("name")
        if not entry_name:
            continue
        if entry_name in base_by_name:
            for k, v in entry.items():
                if v is not None:
                    base_by_name[entry_name][k] = v
        else:
            base_by_name[entry_name] = dict(entry)
    return list(base_by_name.values())


def x__merge__mutmut_8(base: list[dict], override: list[dict]) -> list[dict]:
    """Merge override entries into base by name; unknown override names are appended."""
    base_by_name = {
        c["name"]: dict(c) for c in base if isinstance(c, dict) and "name" not in c
    }
    for entry in override:
        if not isinstance(entry, dict):
            continue
        entry_name = entry.get("name")
        if not entry_name:
            continue
        if entry_name in base_by_name:
            for k, v in entry.items():
                if v is not None:
                    base_by_name[entry_name][k] = v
        else:
            base_by_name[entry_name] = dict(entry)
    return list(base_by_name.values())


def x__merge__mutmut_9(base: list[dict], override: list[dict]) -> list[dict]:
    """Merge override entries into base by name; unknown override names are appended."""
    base_by_name = {
        c["name"]: dict(c) for c in base if isinstance(c, dict) and "name" in c
    }
    for entry in override:
        if isinstance(entry, dict):
            continue
        entry_name = entry.get("name")
        if not entry_name:
            continue
        if entry_name in base_by_name:
            for k, v in entry.items():
                if v is not None:
                    base_by_name[entry_name][k] = v
        else:
            base_by_name[entry_name] = dict(entry)
    return list(base_by_name.values())


def x__merge__mutmut_10(base: list[dict], override: list[dict]) -> list[dict]:
    """Merge override entries into base by name; unknown override names are appended."""
    base_by_name = {
        c["name"]: dict(c) for c in base if isinstance(c, dict) and "name" in c
    }
    for entry in override:
        if not isinstance(entry, dict):
            break
        entry_name = entry.get("name")
        if not entry_name:
            continue
        if entry_name in base_by_name:
            for k, v in entry.items():
                if v is not None:
                    base_by_name[entry_name][k] = v
        else:
            base_by_name[entry_name] = dict(entry)
    return list(base_by_name.values())


def x__merge__mutmut_11(base: list[dict], override: list[dict]) -> list[dict]:
    """Merge override entries into base by name; unknown override names are appended."""
    base_by_name = {
        c["name"]: dict(c) for c in base if isinstance(c, dict) and "name" in c
    }
    for entry in override:
        if not isinstance(entry, dict):
            continue
        entry_name = None
        if not entry_name:
            continue
        if entry_name in base_by_name:
            for k, v in entry.items():
                if v is not None:
                    base_by_name[entry_name][k] = v
        else:
            base_by_name[entry_name] = dict(entry)
    return list(base_by_name.values())


def x__merge__mutmut_12(base: list[dict], override: list[dict]) -> list[dict]:
    """Merge override entries into base by name; unknown override names are appended."""
    base_by_name = {
        c["name"]: dict(c) for c in base if isinstance(c, dict) and "name" in c
    }
    for entry in override:
        if not isinstance(entry, dict):
            continue
        entry_name = entry.get(None)
        if not entry_name:
            continue
        if entry_name in base_by_name:
            for k, v in entry.items():
                if v is not None:
                    base_by_name[entry_name][k] = v
        else:
            base_by_name[entry_name] = dict(entry)
    return list(base_by_name.values())


def x__merge__mutmut_13(base: list[dict], override: list[dict]) -> list[dict]:
    """Merge override entries into base by name; unknown override names are appended."""
    base_by_name = {
        c["name"]: dict(c) for c in base if isinstance(c, dict) and "name" in c
    }
    for entry in override:
        if not isinstance(entry, dict):
            continue
        entry_name = entry.get("XXnameXX")
        if not entry_name:
            continue
        if entry_name in base_by_name:
            for k, v in entry.items():
                if v is not None:
                    base_by_name[entry_name][k] = v
        else:
            base_by_name[entry_name] = dict(entry)
    return list(base_by_name.values())


def x__merge__mutmut_14(base: list[dict], override: list[dict]) -> list[dict]:
    """Merge override entries into base by name; unknown override names are appended."""
    base_by_name = {
        c["name"]: dict(c) for c in base if isinstance(c, dict) and "name" in c
    }
    for entry in override:
        if not isinstance(entry, dict):
            continue
        entry_name = entry.get("NAME")
        if not entry_name:
            continue
        if entry_name in base_by_name:
            for k, v in entry.items():
                if v is not None:
                    base_by_name[entry_name][k] = v
        else:
            base_by_name[entry_name] = dict(entry)
    return list(base_by_name.values())


def x__merge__mutmut_15(base: list[dict], override: list[dict]) -> list[dict]:
    """Merge override entries into base by name; unknown override names are appended."""
    base_by_name = {
        c["name"]: dict(c) for c in base if isinstance(c, dict) and "name" in c
    }
    for entry in override:
        if not isinstance(entry, dict):
            continue
        entry_name = entry.get("name")
        if entry_name:
            continue
        if entry_name in base_by_name:
            for k, v in entry.items():
                if v is not None:
                    base_by_name[entry_name][k] = v
        else:
            base_by_name[entry_name] = dict(entry)
    return list(base_by_name.values())


def x__merge__mutmut_16(base: list[dict], override: list[dict]) -> list[dict]:
    """Merge override entries into base by name; unknown override names are appended."""
    base_by_name = {
        c["name"]: dict(c) for c in base if isinstance(c, dict) and "name" in c
    }
    for entry in override:
        if not isinstance(entry, dict):
            continue
        entry_name = entry.get("name")
        if not entry_name:
            break
        if entry_name in base_by_name:
            for k, v in entry.items():
                if v is not None:
                    base_by_name[entry_name][k] = v
        else:
            base_by_name[entry_name] = dict(entry)
    return list(base_by_name.values())


def x__merge__mutmut_17(base: list[dict], override: list[dict]) -> list[dict]:
    """Merge override entries into base by name; unknown override names are appended."""
    base_by_name = {
        c["name"]: dict(c) for c in base if isinstance(c, dict) and "name" in c
    }
    for entry in override:
        if not isinstance(entry, dict):
            continue
        entry_name = entry.get("name")
        if not entry_name:
            continue
        if entry_name not in base_by_name:
            for k, v in entry.items():
                if v is not None:
                    base_by_name[entry_name][k] = v
        else:
            base_by_name[entry_name] = dict(entry)
    return list(base_by_name.values())


def x__merge__mutmut_18(base: list[dict], override: list[dict]) -> list[dict]:
    """Merge override entries into base by name; unknown override names are appended."""
    base_by_name = {
        c["name"]: dict(c) for c in base if isinstance(c, dict) and "name" in c
    }
    for entry in override:
        if not isinstance(entry, dict):
            continue
        entry_name = entry.get("name")
        if not entry_name:
            continue
        if entry_name in base_by_name:
            for k, v in entry.items():
                if v is None:
                    base_by_name[entry_name][k] = v
        else:
            base_by_name[entry_name] = dict(entry)
    return list(base_by_name.values())


def x__merge__mutmut_19(base: list[dict], override: list[dict]) -> list[dict]:
    """Merge override entries into base by name; unknown override names are appended."""
    base_by_name = {
        c["name"]: dict(c) for c in base if isinstance(c, dict) and "name" in c
    }
    for entry in override:
        if not isinstance(entry, dict):
            continue
        entry_name = entry.get("name")
        if not entry_name:
            continue
        if entry_name in base_by_name:
            for k, v in entry.items():
                if v is not None:
                    base_by_name[entry_name][k] = None
        else:
            base_by_name[entry_name] = dict(entry)
    return list(base_by_name.values())


def x__merge__mutmut_20(base: list[dict], override: list[dict]) -> list[dict]:
    """Merge override entries into base by name; unknown override names are appended."""
    base_by_name = {
        c["name"]: dict(c) for c in base if isinstance(c, dict) and "name" in c
    }
    for entry in override:
        if not isinstance(entry, dict):
            continue
        entry_name = entry.get("name")
        if not entry_name:
            continue
        if entry_name in base_by_name:
            for k, v in entry.items():
                if v is not None:
                    base_by_name[entry_name][k] = v
        else:
            base_by_name[entry_name] = None
    return list(base_by_name.values())


def x__merge__mutmut_21(base: list[dict], override: list[dict]) -> list[dict]:
    """Merge override entries into base by name; unknown override names are appended."""
    base_by_name = {
        c["name"]: dict(c) for c in base if isinstance(c, dict) and "name" in c
    }
    for entry in override:
        if not isinstance(entry, dict):
            continue
        entry_name = entry.get("name")
        if not entry_name:
            continue
        if entry_name in base_by_name:
            for k, v in entry.items():
                if v is not None:
                    base_by_name[entry_name][k] = v
        else:
            base_by_name[entry_name] = dict(None)
    return list(base_by_name.values())


def x__merge__mutmut_22(base: list[dict], override: list[dict]) -> list[dict]:
    """Merge override entries into base by name; unknown override names are appended."""
    base_by_name = {
        c["name"]: dict(c) for c in base if isinstance(c, dict) and "name" in c
    }
    for entry in override:
        if not isinstance(entry, dict):
            continue
        entry_name = entry.get("name")
        if not entry_name:
            continue
        if entry_name in base_by_name:
            for k, v in entry.items():
                if v is not None:
                    base_by_name[entry_name][k] = v
        else:
            base_by_name[entry_name] = dict(entry)
    return list(None)

mutants_x__merge__mutmut['_mutmut_orig'] = x__merge__mutmut_orig # type: ignore # mutmut generated
mutants_x__merge__mutmut['x__merge__mutmut_1'] = x__merge__mutmut_1 # type: ignore # mutmut generated
mutants_x__merge__mutmut['x__merge__mutmut_2'] = x__merge__mutmut_2 # type: ignore # mutmut generated
mutants_x__merge__mutmut['x__merge__mutmut_3'] = x__merge__mutmut_3 # type: ignore # mutmut generated
mutants_x__merge__mutmut['x__merge__mutmut_4'] = x__merge__mutmut_4 # type: ignore # mutmut generated
mutants_x__merge__mutmut['x__merge__mutmut_5'] = x__merge__mutmut_5 # type: ignore # mutmut generated
mutants_x__merge__mutmut['x__merge__mutmut_6'] = x__merge__mutmut_6 # type: ignore # mutmut generated
mutants_x__merge__mutmut['x__merge__mutmut_7'] = x__merge__mutmut_7 # type: ignore # mutmut generated
mutants_x__merge__mutmut['x__merge__mutmut_8'] = x__merge__mutmut_8 # type: ignore # mutmut generated
mutants_x__merge__mutmut['x__merge__mutmut_9'] = x__merge__mutmut_9 # type: ignore # mutmut generated
mutants_x__merge__mutmut['x__merge__mutmut_10'] = x__merge__mutmut_10 # type: ignore # mutmut generated
mutants_x__merge__mutmut['x__merge__mutmut_11'] = x__merge__mutmut_11 # type: ignore # mutmut generated
mutants_x__merge__mutmut['x__merge__mutmut_12'] = x__merge__mutmut_12 # type: ignore # mutmut generated
mutants_x__merge__mutmut['x__merge__mutmut_13'] = x__merge__mutmut_13 # type: ignore # mutmut generated
mutants_x__merge__mutmut['x__merge__mutmut_14'] = x__merge__mutmut_14 # type: ignore # mutmut generated
mutants_x__merge__mutmut['x__merge__mutmut_15'] = x__merge__mutmut_15 # type: ignore # mutmut generated
mutants_x__merge__mutmut['x__merge__mutmut_16'] = x__merge__mutmut_16 # type: ignore # mutmut generated
mutants_x__merge__mutmut['x__merge__mutmut_17'] = x__merge__mutmut_17 # type: ignore # mutmut generated
mutants_x__merge__mutmut['x__merge__mutmut_18'] = x__merge__mutmut_18 # type: ignore # mutmut generated
mutants_x__merge__mutmut['x__merge__mutmut_19'] = x__merge__mutmut_19 # type: ignore # mutmut generated
mutants_x__merge__mutmut['x__merge__mutmut_20'] = x__merge__mutmut_20 # type: ignore # mutmut generated
mutants_x__merge__mutmut['x__merge__mutmut_21'] = x__merge__mutmut_21 # type: ignore # mutmut generated
mutants_x__merge__mutmut['x__merge__mutmut_22'] = x__merge__mutmut_22 # type: ignore # mutmut generated
mutants_x_load_components__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_load_components__mutmut)
def load_components() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_orig() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_1() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error(None)  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_2() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("XXPyYAML not installed; updater unavailable until next restartXX")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_3() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("pyyaml not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_4() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PYYAML NOT INSTALLED; UPDATER UNAVAILABLE UNTIL NEXT RESTART")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_5() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 / 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_6() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1441 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_7() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 61.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_8() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = None
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_9() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent * "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_10() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(None).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_11() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "XXcomponents.yamlXX"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_12() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "COMPONENTS.YAML"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_13() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(None) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_14() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = None
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_15() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(None)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_16() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception(None)
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_17() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("XXbundled_data not foundXX")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_18() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("BUNDLED_DATA NOT FOUND")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_19() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 / 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_20() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1441 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_21() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 61.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_22() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception(None)
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_23() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("XXMalformed YAML fileXX")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_24() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("malformed yaml file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_25() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("MALFORMED YAML FILE")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_26() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 / 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_27() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1441 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_28() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 61.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_29() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_30() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error(None)
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_31() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("XXData is emptyXX")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_32() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_33() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("DATA IS EMPTY")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_34() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 / 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_35() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1441 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_36() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 61.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_37() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = None

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_38() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get(None, [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_39() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", None)

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_40() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get([])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_41() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", )

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_42() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("XXcomponentsXX", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_43() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("COMPONENTS", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_44() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = None
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_45() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = None
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_46() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = True
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_47() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning(None, OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_48() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", None)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_49() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning(OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_50() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", )

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_51() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("XXCannot access override path %sXX", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_52() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_53() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("CANNOT ACCESS OVERRIDE PATH %S", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_54() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = None
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_55() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode | 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_56() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 19:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_57() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    None,
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_58() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    None,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_59() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    None,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_60() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_61() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_62() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_63() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "XXSkipping override %s: writable by group/others (mode %o)XX",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_64() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_65() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "SKIPPING OVERRIDE %S: WRITABLE BY GROUP/OTHERS (MODE %O)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_66() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode | 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_67() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 512,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_68() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = None
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_69() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = True
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_70() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning(None, OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_71() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", None)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_72() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning(OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_73() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", )
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_74() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("XXCannot stat override path %s - skippingXX", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_75() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_76() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("CANNOT STAT OVERRIDE PATH %S - SKIPPING", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_77() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = None

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_78() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = True

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_79() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(None) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_80() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = None
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_81() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(None)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_82() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = None
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_83() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    None,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_84() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    None,
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_85() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_86() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_87() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get(None, []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_88() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", None),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_89() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get([]),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_90() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", ),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_91() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("XXcomponentsXX", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_92() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("COMPONENTS", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_93() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception(None, OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_94() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", None)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_95() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception(OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_96() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", )
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_97() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("XXFailed to load override YAML %sXX", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_98() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("failed to load override yaml %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_99() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("FAILED TO LOAD OVERRIDE YAML %S", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_100() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = None
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_101() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_102() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning(None, entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_103() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", None)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_104() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning(entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_105() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", )
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_106() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("XXComponent entry is not a mapping - skipped: %rXX", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_107() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_108() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("COMPONENT ENTRY IS NOT A MAPPING - SKIPPED: %R", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_109() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            break
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_110() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = None
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_111() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(None)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_112() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_113() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(None)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_114() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_115() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(None):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_116() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind != "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_117() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "XXaptXX" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_118() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "APT" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_119() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            None,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_120() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            None,
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_121() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_122() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_123() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            1,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_124() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name=None,
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_125() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind=None,
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_126() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=None,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_127() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=None,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_128() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_129() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_130() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_131() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_132() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="XXsystemXX",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_133() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="SYSTEM",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_134() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="XXaptXX",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_135() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="APT",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_136() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=2,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_137() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = None
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_138() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) / 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_139() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(None) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_140() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get(None, 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_141() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", None)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_142() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get(1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_143() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", )) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_144() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("XXpoll_interval_minutesXX", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_145() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("POLL_INTERVAL_MINUTES", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_146() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1441)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_147() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 61.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_148() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning(None)
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_149() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("XXInvalid poll_interval_minutes - using 1440XX")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_150() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_151() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("INVALID POLL_INTERVAL_MINUTES - USING 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_152() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = None
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_153() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 / 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_154() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1441 * 60.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_155() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 61.0
    configs.sort(key=lambda c: c.order)
    return configs, poll_seconds


def x_load_components__mutmut_156() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=None)
    return configs, poll_seconds


def x_load_components__mutmut_157() -> tuple[list[ComponentConfig], float]:
    """Load/validate components from bundled YAML merged with the user override.

    Returns (list of ComponentConfig, poll_interval in seconds).
    """
    try:
        import yaml  # noqa: PLC0415
    except ImportError:
        logger.error("PyYAML not installed; updater unavailable until next restart")  # noqa: TRY400
        return [], 1440 * 60.0

    bundled_path = Path(__file__).parent / "components.yaml"
    try:
        with open(bundled_path) as f:  # noqa: PTH123
            bundled_data = yaml.safe_load(f)
    except OSError:
        logger.exception("bundled_data not found")
        return [], 1440 * 60.0
    except yaml.YAMLError:
        logger.exception("Malformed YAML file")
        return [], 1440 * 60.0

    if not isinstance(bundled_data, dict):
        logger.error("Data is empty")
        return [], 1440 * 60.0
    raw_components: list[dict] = bundled_data.get("components", [])

    try:
        override_exists = OVERRIDE_PATH.exists()
    except OSError:
        override_exists = False
        logger.warning("Cannot access override path %s", OVERRIDE_PATH)

    if override_exists:
        try:
            stat = OVERRIDE_PATH.stat()
            if stat.st_mode & 0o022:
                logger.warning(
                    "Skipping override %s: writable by group/others (mode %o)",
                    OVERRIDE_PATH,
                    stat.st_mode & 0o777,
                )
                override_exists = False
        except OSError:
            logger.warning("Cannot stat override path %s - skipping", OVERRIDE_PATH)
            override_exists = False

    if override_exists:
        try:
            with open(OVERRIDE_PATH) as f:  # noqa: PTH123
                override_data = yaml.safe_load(f)
            if isinstance(override_data, dict):
                raw_components = _merge(
                    raw_components,
                    override_data.get("components", []),
                )
        except Exception:
            logger.exception("Failed to load override YAML %s", OVERRIDE_PATH)
    configs: list[ComponentConfig] = []
    for entry in raw_components:
        if not isinstance(entry, dict):
            logger.warning("Component entry is not a mapping - skipped: %r", entry)
            continue
        cfg = _validate_component(entry)
        if cfg is not None:
            configs.append(cfg)
    # Auto-inject system apt component if none configured in YAML.
    if not any(c.kind == "apt" for c in configs):
        configs.insert(
            0,
            ComponentConfig(
                name="system",
                kind="apt",
                order=1,
                apt_exclude=_KERNEL_FIRMWARE_EXCLUDES,
            ),
        )
    try:
        poll_seconds = float(bundled_data.get("poll_interval_minutes", 1440)) * 60.0
    except (TypeError, ValueError):
        logger.warning("Invalid poll_interval_minutes - using 1440")
        poll_seconds = 1440 * 60.0
    configs.sort(key=lambda c: None)
    return configs, poll_seconds

mutants_x_load_components__mutmut['_mutmut_orig'] = x_load_components__mutmut_orig # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_1'] = x_load_components__mutmut_1 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_2'] = x_load_components__mutmut_2 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_3'] = x_load_components__mutmut_3 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_4'] = x_load_components__mutmut_4 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_5'] = x_load_components__mutmut_5 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_6'] = x_load_components__mutmut_6 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_7'] = x_load_components__mutmut_7 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_8'] = x_load_components__mutmut_8 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_9'] = x_load_components__mutmut_9 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_10'] = x_load_components__mutmut_10 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_11'] = x_load_components__mutmut_11 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_12'] = x_load_components__mutmut_12 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_13'] = x_load_components__mutmut_13 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_14'] = x_load_components__mutmut_14 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_15'] = x_load_components__mutmut_15 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_16'] = x_load_components__mutmut_16 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_17'] = x_load_components__mutmut_17 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_18'] = x_load_components__mutmut_18 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_19'] = x_load_components__mutmut_19 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_20'] = x_load_components__mutmut_20 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_21'] = x_load_components__mutmut_21 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_22'] = x_load_components__mutmut_22 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_23'] = x_load_components__mutmut_23 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_24'] = x_load_components__mutmut_24 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_25'] = x_load_components__mutmut_25 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_26'] = x_load_components__mutmut_26 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_27'] = x_load_components__mutmut_27 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_28'] = x_load_components__mutmut_28 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_29'] = x_load_components__mutmut_29 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_30'] = x_load_components__mutmut_30 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_31'] = x_load_components__mutmut_31 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_32'] = x_load_components__mutmut_32 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_33'] = x_load_components__mutmut_33 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_34'] = x_load_components__mutmut_34 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_35'] = x_load_components__mutmut_35 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_36'] = x_load_components__mutmut_36 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_37'] = x_load_components__mutmut_37 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_38'] = x_load_components__mutmut_38 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_39'] = x_load_components__mutmut_39 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_40'] = x_load_components__mutmut_40 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_41'] = x_load_components__mutmut_41 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_42'] = x_load_components__mutmut_42 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_43'] = x_load_components__mutmut_43 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_44'] = x_load_components__mutmut_44 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_45'] = x_load_components__mutmut_45 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_46'] = x_load_components__mutmut_46 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_47'] = x_load_components__mutmut_47 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_48'] = x_load_components__mutmut_48 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_49'] = x_load_components__mutmut_49 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_50'] = x_load_components__mutmut_50 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_51'] = x_load_components__mutmut_51 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_52'] = x_load_components__mutmut_52 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_53'] = x_load_components__mutmut_53 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_54'] = x_load_components__mutmut_54 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_55'] = x_load_components__mutmut_55 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_56'] = x_load_components__mutmut_56 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_57'] = x_load_components__mutmut_57 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_58'] = x_load_components__mutmut_58 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_59'] = x_load_components__mutmut_59 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_60'] = x_load_components__mutmut_60 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_61'] = x_load_components__mutmut_61 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_62'] = x_load_components__mutmut_62 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_63'] = x_load_components__mutmut_63 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_64'] = x_load_components__mutmut_64 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_65'] = x_load_components__mutmut_65 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_66'] = x_load_components__mutmut_66 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_67'] = x_load_components__mutmut_67 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_68'] = x_load_components__mutmut_68 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_69'] = x_load_components__mutmut_69 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_70'] = x_load_components__mutmut_70 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_71'] = x_load_components__mutmut_71 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_72'] = x_load_components__mutmut_72 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_73'] = x_load_components__mutmut_73 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_74'] = x_load_components__mutmut_74 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_75'] = x_load_components__mutmut_75 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_76'] = x_load_components__mutmut_76 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_77'] = x_load_components__mutmut_77 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_78'] = x_load_components__mutmut_78 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_79'] = x_load_components__mutmut_79 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_80'] = x_load_components__mutmut_80 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_81'] = x_load_components__mutmut_81 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_82'] = x_load_components__mutmut_82 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_83'] = x_load_components__mutmut_83 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_84'] = x_load_components__mutmut_84 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_85'] = x_load_components__mutmut_85 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_86'] = x_load_components__mutmut_86 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_87'] = x_load_components__mutmut_87 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_88'] = x_load_components__mutmut_88 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_89'] = x_load_components__mutmut_89 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_90'] = x_load_components__mutmut_90 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_91'] = x_load_components__mutmut_91 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_92'] = x_load_components__mutmut_92 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_93'] = x_load_components__mutmut_93 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_94'] = x_load_components__mutmut_94 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_95'] = x_load_components__mutmut_95 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_96'] = x_load_components__mutmut_96 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_97'] = x_load_components__mutmut_97 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_98'] = x_load_components__mutmut_98 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_99'] = x_load_components__mutmut_99 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_100'] = x_load_components__mutmut_100 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_101'] = x_load_components__mutmut_101 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_102'] = x_load_components__mutmut_102 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_103'] = x_load_components__mutmut_103 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_104'] = x_load_components__mutmut_104 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_105'] = x_load_components__mutmut_105 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_106'] = x_load_components__mutmut_106 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_107'] = x_load_components__mutmut_107 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_108'] = x_load_components__mutmut_108 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_109'] = x_load_components__mutmut_109 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_110'] = x_load_components__mutmut_110 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_111'] = x_load_components__mutmut_111 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_112'] = x_load_components__mutmut_112 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_113'] = x_load_components__mutmut_113 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_114'] = x_load_components__mutmut_114 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_115'] = x_load_components__mutmut_115 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_116'] = x_load_components__mutmut_116 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_117'] = x_load_components__mutmut_117 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_118'] = x_load_components__mutmut_118 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_119'] = x_load_components__mutmut_119 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_120'] = x_load_components__mutmut_120 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_121'] = x_load_components__mutmut_121 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_122'] = x_load_components__mutmut_122 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_123'] = x_load_components__mutmut_123 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_124'] = x_load_components__mutmut_124 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_125'] = x_load_components__mutmut_125 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_126'] = x_load_components__mutmut_126 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_127'] = x_load_components__mutmut_127 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_128'] = x_load_components__mutmut_128 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_129'] = x_load_components__mutmut_129 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_130'] = x_load_components__mutmut_130 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_131'] = x_load_components__mutmut_131 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_132'] = x_load_components__mutmut_132 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_133'] = x_load_components__mutmut_133 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_134'] = x_load_components__mutmut_134 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_135'] = x_load_components__mutmut_135 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_136'] = x_load_components__mutmut_136 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_137'] = x_load_components__mutmut_137 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_138'] = x_load_components__mutmut_138 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_139'] = x_load_components__mutmut_139 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_140'] = x_load_components__mutmut_140 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_141'] = x_load_components__mutmut_141 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_142'] = x_load_components__mutmut_142 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_143'] = x_load_components__mutmut_143 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_144'] = x_load_components__mutmut_144 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_145'] = x_load_components__mutmut_145 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_146'] = x_load_components__mutmut_146 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_147'] = x_load_components__mutmut_147 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_148'] = x_load_components__mutmut_148 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_149'] = x_load_components__mutmut_149 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_150'] = x_load_components__mutmut_150 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_151'] = x_load_components__mutmut_151 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_152'] = x_load_components__mutmut_152 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_153'] = x_load_components__mutmut_153 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_154'] = x_load_components__mutmut_154 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_155'] = x_load_components__mutmut_155 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_156'] = x_load_components__mutmut_156 # type: ignore # mutmut generated
mutants_x_load_components__mutmut['x_load_components__mutmut_157'] = x_load_components__mutmut_157 # type: ignore # mutmut generated
